# Утилиты для работы с Python проектами

Этот проект содержит две полезные утилиты для работы с Python проектами: сборщик Python кода и анализатор структуры папок.

## 📁 Файлы проекта

### 1. `collector.py` - Сборщик Python кода
Утилита для рекурсивного сбора всех Python файлов из директории и её поддиректорий в один текстовый файл.

### 2. `main.py` - Анализатор структуры папок
Утилита для отображения древовидной структуры файлов и папок с поддержкой игнорирования ненужных элементов.

## 🚀 Установка и требования

**Требования:**
- Python 3.6 или выше
- Стандартные библиотеки Python (os, sys, argparse, pathlib)

Установка не требуется - достаточно скачать файлы и запускать их напрямую.

## 📝 Использование

### Сборщик Python кода (`collector.py`)

**Базовое использование:**
```bash
python collector.py <путь_к_директории>
```

**Параметры командной строки:**
- `directory` - корневая директория для поиска (по умолчанию: текущая директория)
- `-o, --output` - имя выходного файла (по умолчанию: `all_code.txt`)
- `-e, --exclude` - директории для исключения (можно указать несколько)

**Примеры:**
```bash
# Собрать код из текущей директории
python collector.py

# Собрать код из указанной директории
python collector.py /path/to/project

# Собрать код с кастомным именем выходного файла
python collector.py -o combined_code.txt

# Собрать код с дополнительными исключениями
python collector.py -e __pycache__ .git venv test_data
```

### Анализатор структуры папок (`main.py`)

**Базовое использование:**
```bash
python main.py <путь_к_папке>
```

**Функциональность:**
- Рекурсивный обход папок
- Автоматическое игнорирование системных файлов и служебных папок
- Ограничение на количество отображаемых элементов (по умолчанию 25)
- Поддержка пользовательских паттернов для игнорирования
- Отображение относительных путей

**Пример:**
```bash
python main.py .
python main.py /path/to/project
```

## ⚙️ Особенности

### Сборщик Python кода:
- Автоматически игнорирует общие служебные директории (`__pycache__`, `.git`, `venv` и др.)
- Сохраняет структуру с указанием имен файлов
- Обрабатывает ошибки чтения файлов
- Поддерживает кодировку UTF-8
- Сортирует файлы для удобства просмотра

### Анализатор структуры папок:
- Игнорирует скрытые файлы и папки (начинающиеся с `.`)
- Поддерживает широкий набор паттернов для игнорирования
- Ограничивает вывод для предотвращения переполнения
- Интерактивный запрос пользовательских паттернов
- Форматированный вывод с указанием типа содержимого

## 🔧 Настройка исключений

### По умолчанию игнорируются:
- Служебные папки: `__pycache__`, `.git`, `.vscode`, `.idea`, `node_modules`
- Виртуальные окружения: `venv`, `env`, `.env`
- Скомпилированные файлы: `*.pyc`, `*.pyo`, `*.so`, `*.dll`
- Системные файлы: `.DS_Store`, `Thumbs.db`
- Временные файлы: `tmp`, `temp`, `cache`, `*.log`
- Папки сборки: `build`, `dist`, `*.egg-info`

## 📊 Пример вывода

### Для сборщика кода:
```
# utils/helpers.py
def helper_function():
    ...

==================================================

# main.py
import sys
...
```

### Для анализатора структуры:
```
project/: ['README.md', 'requirements.txt', 'src', 'tests']
project/src/: ['__init__.py', 'module1.py', 'module2.py']
project/tests/: ['test_module1.py', 'test_module2.py']
```

## 🐛 Обработка ошибок

- Проверка существования указанных путей
- Обработка ошибок доступа к файлам
- Сообщения об ошибках на русском языке
- Продолжение работы при ошибках чтения отдельных файлов

## 📄 Лицензия

Этот проект распространяется как есть. Вы можете свободно использовать и модифицировать код по своему усмотрению.

## 🤝 Вклад в проект

Если вы нашли ошибку или хотите предложить улучшение:
1. Создайте issue с описанием проблемы или предложения
2. Или создайте pull request с изменениями

## 💡 Советы по использованию

1. Перед сборкой кода убедитесь, что у вас есть права на чтение всех файлов
2. Для больших проектов используйте фильтрацию для исключения ненужных директорий
3. Анализатор структуры полезен для быстрого ознакомления с новым проектом
4. Собранный код можно использовать для обучения AI моделей или анализа паттернов
